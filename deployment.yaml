kind: Service
apiVersion: v1
metadata:
  name: bugbountytest2
spec:
  selector:
    app: helloserver
  type: NodePort
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: bugbountytest2
  labels:
    app: helloserver
spec:
  replicas: 1
  selector:
    matchLabels:
      app: helloserver
  template:
    metadata:
      labels:
        app: helloserver
    spec:
      imagePullSecrets:
      - name: bugbountytest2
      containers:
      - name: helloserver
        image: jarkkotesti/update-container
        ports:
        - containerPort: 80
